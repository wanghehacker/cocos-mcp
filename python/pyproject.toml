[project]
name = "cocos-mcp-server"
version = "0.1.0"
description = "MCP server bridge for Cocos Creator 3.8.x"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
cocos-mcp-server = "cocos_mcp_server:main"
